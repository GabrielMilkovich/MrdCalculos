# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code worktrees
.claude/

# Environment variables (NEVER commit credentials)
.env
.env.local
.env.*.local
.env.e2e

# Bun lockfiles (project standardized on npm)
bun.lock
bun.lockb

# Calibration corpus (real PJC files — never commit)
pjc-corpus/*.PJC
pjc-corpus/*.pjc
pjc-corpus/calibration-*.json
calibration-*.json

# PJC files (dados de processos reais — nunca commitar)
*.PJC
*.pjc
docs/*.PJC
public/reports/*.pjc

# PJe-Calc 2.15.1 source (6.7MB, 802 Java files) — referência externa descompilada
# Ver pjecalc-fonte/README.md para detalhes. Mantida no repo como ground-truth
# para o porte 1:1 em src/lib/pjecalc/core/. Somente leitura.
# (Removido do .gitignore — agora commitado)

# Playwright
playwright-report/
test-results/
